<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="6dp"
    android:layout_marginVertical="4dp"
    app:cardCornerRadius="6dp"
    app:cardElevation="2dp"
    app:strokeColor="#E0E0E0"
    app:strokeWidth="1dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Status Bar -->
        <View
            android:id="@+id/status_bar"
            android:layout_width="4dp"
            android:layout_height="0dp"
            android:background="@color/green"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- Sensor Name -->
        <TextView
            android:id="@+id/sensor_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:layout_marginEnd="8dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="Sensor"
            android:textColor="#212121"
            android:textSize="14sp"
            android:textStyle="bold"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toEndOf="@id/status_bar"
            app:layout_constraintEnd_toStartOf="@id/sensor_total_container" />

        <!-- Reading Value with Badge -->
        <TextView
            android:id="@+id/sensor_reading"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:background="@drawable/reading_badge_bg"
            android:paddingHorizontal="8dp"
            android:paddingVertical="2dp"
            android:text="22.5"
            android:textColor="#FFFFFF"
            android:textSize="12sp"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="@id/sensor_name"
            app:layout_constraintTop_toBottomOf="@id/sensor_name" />

        <!-- Time with Icon -->
        <TextView
            android:id="@+id/sensor_reading_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:drawableStart="@drawable/ic_time_mini"
            android:drawablePadding="4dp"
            android:drawableTint="#757575"
            android:text="3:42 PM"
            android:textColor="#757575"
            android:textSize="11sp"
            app:layout_constraintBottom_toBottomOf="@id/sensor_reading"
            app:layout_constraintStart_toEndOf="@id/sensor_reading"
            app:layout_constraintTop_toTopOf="@id/sensor_reading" />

        <!-- Sensor Image (moved below reading) -->
        <ImageView
            android:id="@+id/sensor_image"
            android:layout_width="55dp"
            android:layout_height="55dp"
            android:layout_marginTop="8dp"
            android:layout_marginStart="6dp"
            android:scaleType="centerCrop"
            app:layout_constraintTop_toBottomOf="@id/sensor_reading"
            app:layout_constraintStart_toEndOf="@id/status_bar" />

        <!-- Camera Button (icon changed and positioned on sensor image) -->
        <ImageView
            android:id="@+id/sensor_image_upload_btn"
            android:layout_width="25dp"
            android:layout_height="20dp"
            android:padding="3dp"
            app:tint="@android:color/black"
            app:layout_constraintBottom_toBottomOf="@id/sensor_image"
            app:layout_constraintEnd_toEndOf="@id/sensor_image" />

        <!-- Total Container -->
        <LinearLayout
            android:id="@+id/sensor_total_container"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="12dp"
            android:background="@drawable/total_container_bg"
            android:gravity="center"
            android:orientation="vertical"
            android:paddingHorizontal="12dp"
            android:paddingVertical="6dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="TOTAL"
                android:textColor="#78909C"
                android:textSize="10sp" />

            <TextView
                android:id="@+id/sensor_total"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="438.7"
                android:textColor="#37474F"
                android:textSize="14sp"
                android:textStyle="bold" />
        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>
